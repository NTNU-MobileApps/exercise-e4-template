import 'package:flutter/material.dart';

class E4App extends StatelessWidget {
  const E4App({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Exercise E4',
      home: const Text("TODO - need to create the layout!"),
    );
  }
}
